name: daily_discipline_trainer
description: Daily Discipline Trainer - Complete Fitness App
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.1.1
  shared_preferences: ^2.2.2
  image_picker: ^1.0.7
  http: ^1.2.0
  confetti: ^0.7.0
  fl_chart: ^0.66.2
  intl: ^0.19.0
  path_provider: ^2.1.2
  cupertino_icons: ^1.0.6
  timezone: ^0.9.2
  flutter_local_notifications: ^17.0.0
  health: ^10.0.0
  permission_handler: ^11.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
